<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head.ejs') %>

  <body>
    <%- include('./partials/nav.ejs') %>

      <div class="create-blogs content">
        <form action="/blogs/edit/<%= blog._id %>?_method=PUT" method="POST">
        <!-- <form> -->
          <label for="title">Blog title:</label>
          <input type="text" id="title" name="title" value="<%= blog.title %>">
          <label for="snippet">Blog snippet:</label>
          <input type="text" id="snippet" name="snippet" value="<%= blog.snippet %>">
          <label for="body">Blog body:</label>
          <textarea id="body" name="body"><%= blog.body %></textarea>
          <button>Submit</button>
        </form>
      </div>

      <%- include('./partials/footer.ejs') %>

      <!-- <script>
        const btn = document.querySelector('form');
        const title = document.querySelector('#title')
        const snippet = document.querySelector('#snippet')
        const blogbody = document.querySelector('textarea')
        btn.addEventListener('submit', (e) => {
          e.preventDefault();
          const endpoint = `/blogs/edit/${btn.dataset.doc}`
          // console.log(title.value, snippet.value, blogbody.value)
          fetch(endpoint, {
            method: 'PUT',
            body: { title: title.value, snippet: snippet.value, body: blogbody.value}
          })
          .then((response) => response.json())
          .then((data) => window.location.href = data.redirect)
          .catch((err) => console.log(err))
        })
      </script> -->
  </body>

</html>